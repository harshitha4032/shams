[build]
  command = "npm run build"
  publish = "dist"
  base = "/"

# Redirects for local development only
# For production, you'll need to update the API base URL in your frontend code
# to point to your deployed backend